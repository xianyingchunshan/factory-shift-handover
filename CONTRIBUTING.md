# 贡献与主控调度

仓库所有者担任主控。协作者的邀请不等于任务指派，先在 Issue 中由主控确认负责人和范围，再开分支。

1. 读取 SPEC、任务卡和 AGENTS。不得改变首版范围。
2. 安装本地钩子，针对任务开独立分支，不直接向 main 推送业务改动。
3. 只改任务授权目录；共用定义有缺口，先向主控提契约变更，不私建旁路。
4. 提交 PR，关联 Issue，提供实际测试命令、非零测试结果、失败边界和外部副作用说明。
5. 主控审核后决定是否合并。不要自动合并或自行关闭需要主控真实验收的卡。

主控本机部署、真实账号授权、真实人员名单与交接数据、最终联调不派给协作者。协作者可写工具，在隔离环境验证，不索要主控凭据。

所有公开材料不得含个人署名、个人绝对路径、真实人员/群/表 ID、真实交接记录、访问链接或密钥。源码中的合成测试标识必须显式标明。Git 作者元数据正常保留。

仓库 guard 是保守检查，不覆盖所有敏感信息形态；提交者必须人工检查 diff，后续可加入专用秘密扫描。CI 通过只代表仓库检查通过，不代表业务可用。

主控审核是协作规定；仅配置 PR 和 CI 门禁并不能保证只有主控能点合并，平台权限及更细合并限制须另行验证。不得声称文档约定自动成为平台权限。
