# 需求与验收（第二版口径）

> 2026-09-20 主控拍板：架构重构为"输入 → 钉钉AI表格记录 → 输出"三段独立。最终口径以主控在 Issue 中确认的为准；已确认口径优先于本文件。

## 0. 总架构：三段独立

```
① 交接班输入 → ② 钉钉AI表格记录 → ③ 交接班输出
```

- 输入只管"把事记全记准"；表格只管"存和校验，不完整就告警"；输出只管"从表生成人能看懂的时间顺序清单"。
- 载体：钉钉（AI表格 / 待办 / 消息）。可信身份源不唯一——钉钉/EAM/EHR 均可，三源已经 Probe-01 实测成立（Issue #2，docs/evidence/probe-01.md）。

## 1. 输入规范（九类，已拍板）

每个事项一条记录，统一字段：**事项描述、发生时间（必填，精确到分）、类别、重要级（关键/一般）、状态（进行中/已办/移交接班人）、责任人、关联编号、备注**。

| # | 类别 | 具体记什么 |
|---|---|---|
| 1 | 运行工况 | 实发功率/负荷、AGC/AVC 投切、限电指令及执行、并网/解列、调度指令 |
| 2 | 设备情况 | 风机/光伏运行·停机·故障台数、主变/集电线路/SVG/箱变异常、故障处理进展 |
| 3 | 缺陷完成情况 | 本班新增缺陷、已消缺、处理进展（关联 EAM 缺陷编号、等级） |
| 4 | 隐患完成情况 | 新增隐患、整改进展、超期提醒（A/B/C 级，关联台账编号） |
| 5 | 重大事项 | 重大隐患、重大操作、机组跳闸、安全事件（默认=关键级，不可降） |
| 6 | 两票执行 | 工作票/操作票：开工、执行中、终结（关联票号） |
| 7 | 作业与外委 | 检修作业、外委施工、动火等现场活动及监护情况 |
| 8 | 上级指令与通知 | 公司/调度指示传达、临时要求 |
| 9 | 其他 | 气象预警（大风/寒潮/暴雨）及应对、车辆物资、人员缺岗替班、交接备注 |

关键级强制规则：第 5 类默认关键级；状态填"移交接班人"的事项自动升级关键级。

**接班人确定（2026-09-21 主控拍板）**：交班人（第一责任人）固定，接班人（第二责任人）**不由配置写死**——建班次时作为**前置问题**询问"本班接班人是谁？"，答复经钉钉解析为唯一可信身份后写入班次记录。**允许交接班换人**：班次归档前可修改接班人，修改须显式操作并留痕（谁、何时、改成谁），不静默；唯一硬约束是**新接班人在钉钉找得到**（可解析为唯一可信身份），解析不到即拒绝变更。接班人变更后，确认待办与提醒路由跟随新接班人，已发旧人的待办显式作废并重发，不静默残留。

## 2. 钉钉AI表格记录层（已拍板）

两张表：**班次表**（日期、班次、交接线、交班人、接班人、状态）+ **交接事件表**（统一字段 + 班次 ID 关联）。

完整性告警四条硬校验：

| 校验 | 规则 | 时机 |
|---|---|---|
| 时间缺失 | 发生时间为空 → 拒收 + 告警 | 录入时即时 |
| 时间不完整 | 只有日期没有时:分 → 告警补全 | 录入时即时 |
| 时间越界 | 时间不在本班次区间 → 告警复核 | 录入时即时 |
| 关键字段缺失 | 类别/重要级/状态为空 → 告警 | 交班提交前全表复验 |

告警钉钉直发交班人；**交班提交前全表复验有告警的，不允许生成输出清单**。

## 3. 输出（已拍板）

人能看懂的清单，按时间顺序完整呈现，重要事件重点标记：

1. 重要事项（关键级，置顶加粗）
2. 事件流水（全量，按时间顺序，含类别/状态标记）
3. 未完事项移交接班人（N 项，接班人须逐项确认）
4. 完整性校验结果（事件数、时间完整率、告警数）
5. 确认区

**分发口径：只单发给交班人和接班人两人，不进群。**

## 4. 接班确认（已拍板）

钉钉待办承载：关键事项逐项发待办、一般事务整单发待办；完成后**回写 AI 表格状态**。以操作者留痕为准——写入不等于完成，回读才算。

## 5. 通用护栏（沿袭第一版拍板，继续有效）

- 一条交接线一班一套记录；同班多线各成一套。
- 防重：同班同线不重复建班次；重复提交不建第二单。
- 外部写入结果不明先回查，不盲目重发。
- 所有时间含时区（Asia/Shanghai）。
- 配置快照：班次建立时锁定交接线/关键事项标准；变更须显式确认，不静默改历史记录。**接班人为例外：按"接班人确定"条款允许显式换人**（前置询问写入 + 钉钉可解析 + 变更留痕）。
- 人工停止点：授权、人员配置、交班填写、接班确认——缺输入不可代填。

## 6. 权责

- 主控（仓库所有者）：GitHub 派工、口径确认、本机实测、合并与发布决策。
- 协作者：隔离开发和测试，不操作主控机器、不索取主控凭据。
- 交班人 / 接班人：真实填写与确认；确认不由脚本代勾。

## 7. 验收合同

- 等级：L1 隔离单元测试；L2 进程/模块集成；L3 界面操作；L4 真实外部系统回读。协作者最多交付 L2/L3 证据，L4 由主控执行。
- 成功证据：真实表格记录 ID、待办 ID、可信操作人、状态回读、消息送达与不重复证据。
- 反向验证：时间缺失/不完整/越界、关键字段缺失、重复提交、未确认不改状态、结果不明、重启恢复。
- 不完成：只有脚本退出码、模拟响应、文档检查或 Agent 自报成功。

## 8. 后续保留

多人会签、跨班统计、历史检索、交接考核、群发输出、移动端均延期。当前异常不得因功能未实现而自动确认。
