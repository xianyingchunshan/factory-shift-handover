# 需求与验收（草案）

> 本文件是草案。范围与规则的最终口径由主控在 Issue 中确认；未确认前不实施。已确认口径优先于本文件。

## 1. 范围

总目标：厂级人员工作交接的线上闭环。交班人把本班情况、未完事项、注意事项按固定结构填写交接单，接班人逐项确认，双方确认留痕可查可回溯。

MVP 只做单一场景：一个厂/站、固定交接方向（交班人 → 接班人）、一班一张交接单。多人会签、跨班统计、历史检索、交接质量考核均为后续事项，不进首版。

首版固定为一台使用电脑、单实例、一套交接台账、一笔完整交接。不建设通用平台，不接其他企业业务系统。

## 2. 首版业务规则（候选，逐条待主控确认）

1. 一班一张交接单；交接事项逐条列出，每条含：事项描述、状态（进行中/已办/移交接班人）、备注。
2. 交班提交必须有可信提交身份；字段来自真实输入，不猜造。自动记录 ID 不冒充人员身份。
3. 接班确认逐项进行；未确认事项显式挂起，不自动视为已交接。确认动作关联原交接单。
4. 双方确认完成后交接单归档；重复提交不建第二单。外部写入结果不明先回查，不盲目重发。
5. 提醒：临近交班未提交提醒交班人；存在未确认事项提醒接班人；同一事项防重，不催交接双方以外的无关人员。所有时间含时区。
6. 交接单保留创建时的人员配置；运行中变更身份路由必须显式确认，不静默改历史单。

## 3. 部署硬闸门（候选）

- 使用者本机部署，程序检测并列出缺项；环境、身份、人员配置、权限分别核验，最终绑定由使用者明确确认。
- 缺配置或权限时不能启动；所有业务入口调用底层闸门，不依赖提示词或可编辑的就绪标记。
- 导入或创建前展示预览，确认后才写入，写入后回读；重复部署不覆盖既有数据。敏感信息不进日志或普通配置文件。

## 4. 权责

- 主控（仓库所有者）：GitHub 派工、口径确认、本机实测、合并与发布决策。
- 协作者：隔离开发和测试，不操作主控机器、不索取主控凭据。
- 交班人 / 接班人：真实填写与确认；确认不由脚本代勾。

## 5. 先验证后依赖

外部能力（确认渠道、提醒送达、提交身份可信度）先经调查卡（Probe）验证成立，主控确认后才冻结契约；能力不成立则停止，不猜造外部能力，不悄悄降低验收。未验证事实保留在 docs/evidence（仅脱敏结论），不用缓存或转述替代实测。

## 6. 验收合同

- 等级：L1 隔离单元测试；L2 进程/模块集成；L3 界面操作；L4 真实外部系统回读。协作者最多交付 L2/L3 证据，L4 由主控执行。
- 成功证据：真实单据 ID、各阶段确认记录 ID、可信操作人、交接项状态回读、提醒送达与不重复证据。
- 人工停止点：授权、人员与配置确认、交接填写、双方确认。缺输入不可代填。
- 反向验证：缺字段、错人、重复提交、未确认不改状态、结果不明、重启恢复；确认不齐不归档。
- 不完成：只有脚本退出码、模拟响应、文档检查或 Agent 自报成功。

## 7. 后续保留

多人会签、跨班统计、历史检索、移动端、复杂角色矩阵、通用审批框架均延期。当前异常不得因功能未实现而自动确认。
