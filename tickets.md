# 任务与主控调度

主控分配后才开工，禁止自动领取。GitHub Issues 为执行状态入口，本文件保留范围和依赖；每张卡完成必须附测试证据。L1 为隔离单元测试，L2 为进程/模块集成，L3 为界面操作，L4 为真实外部系统回读。本文件不自行关闭 Issue、不替主控勾选验收。

以下为首轮卡草案，主控在 Issue 中确认负责人和范围后才生效。

## Probe-01 主控本机：验证交接确认与提醒的最小真实链路

**执行位置：主控本机，不外派。依赖：无。**
目标：验证"可信身份读取（身份源不唯一：钉钉/EAM/EHR 等候选，逐个实测）、确认待办可指向指定接班人、提醒可送达指定人"的最短真实链路。与单据无关的人员全拒绝。
允许改：docs/evidence（仅脱敏结论）。禁止改：业务源码、他人机器、生产记录。
验收 L4：真实待办 ID、确认操作者证据、精确回读；验证错人、无关联单据不放行。没有实际操作者证据则报告阻塞，不伪造关联。重复运行先回查，不重复创建。测试记录清理并回读。
- [ ] 可信身份、确认结果、提醒送达均有证据
- [ ] 权限隔离和清理已核验
- [ ] 不成立的能力明确提出替代建议，未悄悄降低门槛

## T01 公共契约：班次、事件记录、告警规则、清单与确认回写

**执行位置：隔离开发，可外派。依赖：Probe-01。**
目标：按 SPEC 第二版三段架构，冻结班次/交接事件记录的统一字段、四条完整性告警规则、输出清单结构与确认回写状态机（含钉钉AI表格字段映射），并提供合成测试，使后续模块不用猜字段。主控冻结后才能作为共同依赖。
允许改：contracts/、tests/contracts/、SPEC.md、tickets.md。
禁止改：CI、scripts/ 检查口径、各业务模块、本机配置。
验收 L1：缺时间/时间不完整/越界必告警；重复提交不双写；关键级强制升级（第5类与"移交接班人"）；告警未清不允许生成清单；结果不明只回查不重放。
- [ ] 状态、字段、错误、幂等有一致定义
- [ ] 其他模块可仅依赖契约开发
- [ ] 测试非零且通过

## T02 三段实现：输入校验、表格读写、清单生成与待办回写（占位）

**执行位置：隔离开发，可外派。依赖：T01。**
目标：按冻结契约实现三段——①输入校验（九类字段、四条告警）②钉钉AI表格读写（班次表/交接事件表）③清单生成（时间序、关键级置顶）+ 钉钉待办确认与回写。真实联调依赖主控授权的隔离表格目标。
允许改：workflow/、tests/workflow/、integrations/aitable/、tests/integrations/。
禁止改：部署、确认渠道连接层、他人定义的字段。
验收 L1/L2：告警拦截可复现；清单与表格记录一致；待办完成后回写状态正确；错人、缺字段、重复提交不放行；确认前状态不变；重启后状态可恢复。真实表格读写属 L4，由主控执行。
- [ ] 正常闭环及拒绝路径可复现
- [ ] 重复动作不重复生效有测试
