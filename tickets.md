# 任务与主控调度

主控分配后才开工，禁止自动领取。GitHub Issues 为执行状态入口，本文件保留范围和依赖；每张卡完成必须附测试证据。L1 为隔离单元测试，L2 为进程/模块集成，L3 为界面操作，L4 为真实外部系统回读。本文件不自行关闭 Issue、不替主控勾选验收。

以下为首轮卡草案，主控在 Issue 中确认负责人和范围后才生效。

## T03 契约补充：接班人前置询问与换人变更

**执行位置：隔离开发，可外派。依赖：T01（已冻结）。**
目标：按 SPEC"接班人确定"条款（2026-09-21 拍板）增补契约与实现——contracts 增补接班人变更方法（钉钉解析唯一可信身份、显式留痕、归档/blocked 态拒绝变更）；workflow 层实现建班次前置询问入口、确认待办与提醒路由跟随新接班人、旧待办显式作废重发。
允许改：contracts/（仅增补，不改已冻结五类契约的既有语义）、tests/contracts/、workflow/、tests/workflow/、integrations/aitable/、tests/integrations/、SPEC.md、tickets.md。
禁止改：CI、scripts/ 检查口径、docs/、本机配置。
验收 L1/L2：解析不到身份拒变更；归档与 blocked 态拒变更；变更留痕（谁/何时/改成谁）可回读；路由跟随（新接班人收到待办，旧待办作废）；重启恢复后变更记录不丢；测试非零且通过。
真实钉钉解析与真实待办重发属 L4，由主控执行。

## Probe-01 主控本机：验证交接确认与提醒的最小真实链路

**执行位置：主控本机，不外派。依赖：无。**
目标：验证"可信身份读取（身份源不唯一：钉钉/EAM/EHR 等候选，逐个实测）、确认待办可指向指定接班人、提醒可送达指定人"的最短真实链路。与单据无关的人员全拒绝。
允许改：docs/evidence（仅脱敏结论）。禁止改：业务源码、他人机器、生产记录。
验收 L4：真实待办 ID、确认操作者证据、精确回读；验证错人、无关联单据不放行。没有实际操作者证据则报告阻塞，不伪造关联。重复运行先回查，不重复创建。测试记录清理并回读。
- [ ] 可信身份、确认结果、提醒送达均有证据
- [ ] 权限隔离和清理已核验
- [ ] 不成立的能力明确提出替代建议，未悄悄降低门槛

## T01 公共契约：班次、事件记录、告警规则、清单与确认回写

**执行位置：隔离开发，可外派。依赖：Probe-01。**
目标：按 SPEC 第二版三段架构，冻结班次/交接事件记录的统一字段、四条完整性告警规则、输出清单结构与确认回写状态机（含钉钉AI表格字段映射），并提供合成测试，使后续模块不用猜字段。主控冻结后才能作为共同依赖。
允许改：contracts/、tests/contracts/、SPEC.md、tickets.md。
禁止改：CI、scripts/ 检查口径、各业务模块、本机配置。
验收 L1：缺时间/时间不完整/越界必告警；重复提交不双写；关键级强制升级（第5类与"移交接班人"）；告警未清不允许生成清单；结果不明只回查不重放。
- [ ] 状态、字段、错误、幂等有一致定义
- [ ] 其他模块可仅依赖契约开发
- [ ] 测试非零且通过

## T02 三段实现：输入校验、表格读写、清单生成与待办回写（占位）

**执行位置：隔离开发，可外派。依赖：T01。**
目标：按冻结契约实现三段——①输入校验（九类字段、四条告警）②钉钉AI表格读写（班次表/交接事件表）③清单生成（时间序、关键级置顶）+ 钉钉待办确认与回写。真实联调依赖主控授权的隔离表格目标。
允许改：workflow/、tests/workflow/、integrations/aitable/、tests/integrations/。
禁止改：部署、确认渠道连接层、他人定义的字段。
验收 L1/L2：告警拦截可复现；清单与表格记录一致；待办完成后回写状态正确；错人、缺字段、重复提交不放行；确认前状态不变；重启后状态可恢复。真实表格读写属 L4，由主控执行。
- [ ] 正常闭环及拒绝路径可复现
- [ ] 重复动作不重复生效有测试

## T04 契约适配：驻场期语义（交接周期=轮换）

**执行位置：隔离开发，可外派。依赖：T03。**
目标：把"一班一张单"语义适配为"一次轮换一张单"（SPEC §0 项目定位）——契约层按**只增不改**原则增补（"驻场期"选项/等价表达；文档明确 start/end=驻场期边界、E003 越界边界=驻场期），工作流与清单标题口径跟随（如"交接班清单·驻场期"）。AI表格的选项更新属 L4，由主控执行。
允许改：contracts/（仅增补）、tests/contracts/、workflow/、tests/workflow/、SPEC.md、tickets.md。
禁止改：CI、scripts/ 检查口径、docs/、本机配置。
验收 L1/L2：驻场期边界下 E001–E004 全回归；长周期（如 30 天）与跨零点边界有测试；清单标题/汇总口径跟随；既有语义回归全绿。
- [ ] 契约只增不改，回归全绿
- [ ] 长周期与跨零点边界有测试
- [ ] 清单口径跟随驻场期

## T05 九类自动化①：缺陷+隐患自动拉取（EAM → 交接事件表）

**执行位置：隔离开发，可外派；真实 EAM 读取属 L4，由主控执行。依赖：T02。**
目标：从 EAM 拉取本驻场期内的缺陷与隐患记录，按九类统一字段与四条告警规则转换为交接事件候选行（category=defect/hazard），供人工确认与补"处理进展"；**只读拉取，不写 EAM**；真实接口以合成夹具隔离测试。
允许改：integrations/、tests/integrations/、workflow/、tests/workflow/。
禁止改：contracts/、CI、scripts/ 检查口径、本机配置。
验收 L1/L2：字段映射（编号/等级/状态/时间）与告警联动正确；缺时间/越界按四条规则处理；重复拉取幂等；合成夹具测试非零且通过。
- [ ] 映射与告警联动有测试
- [ ] 重复拉取幂等
- [ ] 只读拉取（不写外部系统）

## T06 skill 封装：输入→过程→输出可运行交付

**执行位置：隔离开发，可外派。依赖：T04（建议先行，口径对齐后封装更稳）。**

> 原 Probe-02（群机器人关键词监听）已按主控 2026-09-21 拍板**取消**：外部触发由使用方自行配置，本仓交付标准=skill 能跑通输入→过程→输出三步。

目标：把三段能力（输入校验 → AI表格读写 → 清单输出）封装为可独立运行的 skill——入口脚本 + SKILL.md（输入/输出契约、配置项、告警语义、失败处置），端到端可跑。**不包含机器人/关键词触发**。
允许改：新增 skill 目录（含 SKILL.md）、tests/skill/（或等价）、docs/。
禁止改：contracts/、CI、scripts/ 检查口径、本机配置。
验收 L1/L2：端到端跑通（给定输入 → 校验 → 表格 → 清单）；告警拦截路径可复现；不接真实外部系统（合成/本地夹具）；SKILL.md 写明输入输出契约、配置项与未覆盖边界。
- [ ] 端到端可跑且可复现
- [ ] SKILL.md 契约说明齐全
- [ ] 明确不含机器人监听/触发（范围外）
